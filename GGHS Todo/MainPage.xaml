<Page
    x:Class="GGHS_Todo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GGHS_Todo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Page.Resources>
        <SolidColorBrush x:Key="txtColor" Color="Black" />
        <SolidColorBrush x:Key="dayColor" Color="LightSteelBlue" />
        <CornerRadius x:Key="corner">10</CornerRadius>
        <SolidColorBrush x:Key="cellColor" Color="#EEF4F4F4" />
        <x:String x:Key="gridThick">5</x:String>
        <x:Int32 x:Key="cellFontSize">27</x:Int32>
        <x:Int32 x:Key="comboFontSize">25</x:Int32>
    </Page.Resources>

    <Grid>
        <Grid Margin="177,65,0,0" HorizontalAlignment="Left" Width="469" Height="118" VerticalAlignment="Top">
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="Foreground" Value="{StaticResource txtColor}"/>
                    <Setter Property="FontFamily" Value="Segoe" />
                </Style>
            </Grid.Resources>
            <TextBlock x:Name="mainText" Text="Codename Bracelet" FontSize="55" Height="73" FontWeight="Bold"/>
            <TextBlock x:Name="mainText2" Text="Development is underway" FontSize="30" Width="544" Foreground="#FF444444" Margin="0,77,0,0"/>
        </Grid>
        <Grid Margin="10,205,1595,10">
            <Button Margin="0,115,0,0" VerticalAlignment="Top" Height="60" Width="206" HorizontalAlignment="Center" CornerRadius="10"/>
        </Grid>

        <ScrollViewer Margin="325,205,10,10">
            <Grid>
                <Grid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="CornerRadius" Value="10"/>
                        <Setter Property="VerticalAlignment" Value="Top" />
                    </Style>
                </Grid.Resources>
                <Button>
                    <Grid Height="80" VerticalAlignment="Center" HorizontalAlignment="Left" Width="2000" Margin="-12,0,0,0" >
                        <TextBlock FontSize="17" Text="화학Ⅱ" Margin="80,12,0,44" HorizontalAlignment="Left" Width="2000"/>
                        <TextBlock FontSize="15" Text="반응 엔탈피 수행평가" Margin="80,43,0,13" HorizontalAlignment="Left" Width="2000" Foreground="#ffa4a4a4"/>
                    </Grid>
                </Button>
            </Grid>
        </ScrollViewer>

    </Grid>
</Page>
